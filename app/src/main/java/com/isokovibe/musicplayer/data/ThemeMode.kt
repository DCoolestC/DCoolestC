package com.isokovibe.musicplayer.data

/** iSokoVibe's brand identity is bold red on black, so DARK is the default look. */
enum class ThemeMode(val label: String) {
    DARK("Vibe (dark)"),
    LIGHT("Light"),
    SYSTEM("Follow system")
}
